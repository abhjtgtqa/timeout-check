name: workflow timeout check
on:
  push:
    branches: "*"
  workflow_dispatch:
jobs:
  timeout:
    runs-on: ubuntu-latest
    steps:
      - name: test
        uses: actions/checkout@v4.2.2
      - name: two
        run: echo "Test"
